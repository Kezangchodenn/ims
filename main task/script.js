function myDeleteFunction() {
    if(deleteRow = 2 ){
        document.querySelector(".table").deleteRow(2);
    }
    else if(deleteRow = 3 ){
        document.querySelector(".table").deleteRow(3);
    }
    
  }